// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.25.0
// source: batch.go

package property_repo

import (
	"context"
	"errors"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5"
)

var (
	ErrBatchAlreadyClosed = errors.New("batch already closed")
)

const deleteSelectOption = `-- name: DeleteSelectOption :batchexec
DELETE FROM select_options WHERE id = $1
`

type DeleteSelectOptionBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

func (q *Queries) DeleteSelectOption(ctx context.Context, id []uuid.UUID) *DeleteSelectOptionBatchResults {
	batch := &pgx.Batch{}
	for _, a := range id {
		vals := []interface{}{
			a,
		}
		batch.Queue(deleteSelectOption, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &DeleteSelectOptionBatchResults{br, len(id), false}
}

func (b *DeleteSelectOptionBatchResults) Exec(f func(int, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		if b.closed {
			if f != nil {
				f(t, ErrBatchAlreadyClosed)
			}
			continue
		}
		_, err := b.br.Exec()
		if f != nil {
			f(t, err)
		}
	}
}

func (b *DeleteSelectOptionBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}
