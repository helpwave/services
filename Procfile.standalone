# Format; See https://devcenter.heroku.com/articles/procfile#procfile-format
# <process type / name>: <command>
daprd: daprd --log-level debug --app-id $DAPR_APP_ID --config dapr.yaml --app-port 80 --dapr-internal-grpc-port 35001
app: ./app
